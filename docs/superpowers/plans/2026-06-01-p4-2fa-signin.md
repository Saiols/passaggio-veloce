# P4 · Check 2FA al sign-in — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Far interrogare il codice 2FA TOTP al login per gli utenti che hanno il 2FA attivo (oggi il setup esiste ma il sign-in non lo verifica), con supporto ai backup code.

**Architecture:** Verifica 2FA centralizzata in `authorize` (auth.ts) come fonte autoritativa (consuma anche il backup code). `loginAction` fa un pre-check password per decidere se mostrare il campo codice (`needTotp`) senza esporre la password al client. Il form login diventa controllato (gli input sopravvivono al re-render del passo TOTP). Core di verifica isolato in un helper async testabile con TOTP-verifier iniettabile.

**Tech Stack:** Auth.js v5 (Credentials), Next.js 16, otplib (già usato), bcryptjs, Vitest.

Spec: `docs/superpowers/specs/2026-06-01-completamenti-locali-design.md` (§P4).

## Repo facts (verificati)
- `apps/piattaforma/src/lib/auth/totp.ts`: `verifyTotpCode(secret, code): boolean`, `verifyBackupCode(hashes: string[], code): Promise<number>` (indice consumato o -1). `User` ha `twoFactorEnabled: boolean`, `twoFactorSecret: string|null`, `twoFactorBackupCodes: Json` (array di hash bcrypt).
- `apps/piattaforma/src/auth.ts`: provider `Credentials` con `credentialsSchema = z.object({ email, password })`; `authorize` trova i candidati `prisma.user.findMany({ where:{ email, deletedAt:null, status:{not:'SUSPENDED'} }, include:{company:true}})` (ritorna tutti gli scalari, inclusi i campi 2FA), confronta bcrypt, ritorna l'oggetto user o null.
- `apps/piattaforma/src/app/(auth)/actions.ts`: `loginAction(prev, formData)` con rate-limit, poi `signIn('credentials', { email, password, redirectTo })`; `LoginActionState = { error?: string }`.
- `apps/piattaforma/src/app/(auth)/login/login-form.tsx`: client, `useActionState(loginAction, {})`, campi email/password non controllati.
- Test esistente `(auth)/actions.test.ts` testa solo `registerAction` (mocka `@/auth` signIn, prisma) — non chiama loginAction, quindi non si rompe.

## File Structure
- Modify `apps/piattaforma/src/lib/auth/totp.ts` — aggiunge `verifyTwoFactor` (+ test nuovo file).
- Create `apps/piattaforma/src/lib/auth/totp.test.ts` — test di `verifyTwoFactor`.
- Modify `apps/piattaforma/src/auth.ts` — credenziale `totp` + enforcement 2FA in authorize.
- Modify `apps/piattaforma/src/app/(auth)/actions.ts` — pre-check `needTotp` in loginAction.
- Modify `apps/piattaforma/src/app/(auth)/login/login-form.tsx` — input controllati + campo codice.

---

### Task 1: Helper `verifyTwoFactor` (testabile, TDD)

**Files:**
- Modify `apps/piattaforma/src/lib/auth/totp.ts`
- Create `apps/piattaforma/src/lib/auth/totp.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// apps/piattaforma/src/lib/auth/totp.test.ts
import { describe, it, expect } from 'vitest';
import { verifyTwoFactor, hashBackupCodes } from './totp';

describe('verifyTwoFactor', () => {
  it('returns ok via TOTP when the injected verifier accepts the code', async () => {
    const r = await verifyTwoFactor(
      { secret: 'SECRET', backupCodeHashes: [] },
      '123456',
      () => true,
    );
    expect(r).toEqual({ ok: true, consumedBackupIndex: null });
  });

  it('falls back to a backup code and reports the consumed index', async () => {
    const hashes = await hashBackupCodes(['AAAA-BBBB', 'CCCC-DDDD']);
    const r = await verifyTwoFactor(
      { secret: 'SECRET', backupCodeHashes: hashes },
      'cccc-dddd', // case-insensitive, second code
      () => false, // TOTP rejects
    );
    expect(r.ok).toBe(true);
    expect(r.consumedBackupIndex).toBe(1);
  });

  it('rejects an empty code', async () => {
    const r = await verifyTwoFactor({ secret: 'SECRET', backupCodeHashes: [] }, '   ', () => true);
    expect(r).toEqual({ ok: false, consumedBackupIndex: null });
  });

  it('rejects an invalid code (no TOTP, no backup match)', async () => {
    const hashes = await hashBackupCodes(['AAAA-BBBB']);
    const r = await verifyTwoFactor({ secret: 'SECRET', backupCodeHashes: hashes }, 'ZZZZ-9999', () => false);
    expect(r).toEqual({ ok: false, consumedBackupIndex: null });
  });

  it('rejects when secret missing and no backup match', async () => {
    const r = await verifyTwoFactor({ secret: null, backupCodeHashes: [] }, '123456', () => true);
    expect(r).toEqual({ ok: false, consumedBackupIndex: null });
  });
});
```

> Nota: l'iniezione del TOTP-verifier evita di dover generare un token TOTP reale nel test. Il caso `secret: null` non deve nemmeno chiamare il verifier (niente secret → niente TOTP).

- [ ] **Step 2: Run test → FAIL**

Run: `pnpm --filter piattaforma test -- totp`

- [ ] **Step 3: Implement** — aggiungi in fondo a `totp.ts`:

```ts
/**
 * Verifica un codice 2FA: prima come TOTP (se c'è un secret), poi come backup
 * code. Ritorna `consumedBackupIndex` (>=0) se è stato usato un backup code,
 * così il chiamante può rimuoverlo. Il TOTP-verifier è iniettabile per i test.
 */
export async function verifyTwoFactor(
  input: { secret: string | null; backupCodeHashes: string[] },
  code: string,
  totpVerify: (secret: string, code: string) => boolean = verifyTotpCode,
): Promise<{ ok: boolean; consumedBackupIndex: number | null }> {
  const c = code.trim();
  if (!c) return { ok: false, consumedBackupIndex: null };
  if (input.secret && totpVerify(input.secret, c)) {
    return { ok: true, consumedBackupIndex: null };
  }
  const idx = await verifyBackupCode(input.backupCodeHashes, c);
  if (idx >= 0) return { ok: true, consumedBackupIndex: idx };
  return { ok: false, consumedBackupIndex: null };
}
```

- [ ] **Step 4: Run test → PASS**

Run: `pnpm --filter piattaforma test -- totp`

- [ ] **Step 5: Commit**

```bash
git add apps/piattaforma/src/lib/auth/totp.ts apps/piattaforma/src/lib/auth/totp.test.ts
git commit -m "feat(auth): verifyTwoFactor helper (TOTP + backup code) + test"
```

---

### Task 2: Enforcement 2FA in `authorize`

**Files:** Modify `apps/piattaforma/src/auth.ts`

- [ ] **Step 1: Add the totp credential + schema field**

In `credentialsSchema` aggiungi `totp`:
```ts
const credentialsSchema = z.object({
  email: z.string().email(),
  password: z.string().min(1),
  totp: z.string().optional(),
});
```
Nel blocco `credentials: { ... }` del provider aggiungi:
```ts
        totp: { label: 'Codice 2FA', type: 'text' },
```

- [ ] **Step 2: Import the helper**

In testa: `import { verifyTwoFactor } from '@/lib/auth/totp';`

- [ ] **Step 3: Enforce 2FA after password match**

Dopo il blocco che individua `matched` (subito dopo `if (!matched) return null;` e PRIMA dell'`await prisma.user.update({ ... lastLoginAt ... })`), inserisci:

```ts
        // P4: se il 2FA è attivo, il codice TOTP (o un backup code) è obbligatorio.
        // authorize è la fonte autoritativa: consuma il backup code se usato.
        if (matched.twoFactorEnabled) {
          const code = parsed.data.totp ?? '';
          const backupHashes = Array.isArray(matched.twoFactorBackupCodes)
            ? (matched.twoFactorBackupCodes as string[])
            : [];
          const res = await verifyTwoFactor(
            { secret: matched.twoFactorSecret, backupCodeHashes: backupHashes },
            code,
          );
          if (!res.ok) return null;
          if (res.consumedBackupIndex !== null) {
            const remaining = backupHashes.filter((_, i) => i !== res.consumedBackupIndex);
            await prisma.user.update({
              where: { id: matched.id },
              data: { twoFactorBackupCodes: remaining },
            });
          }
        }
```

> `parsed.data.totp` esiste ora che lo schema lo include. `matched` proviene dal `findMany` con tutti gli scalari, quindi `twoFactorEnabled/Secret/BackupCodes` sono presenti.

- [ ] **Step 4: Typecheck**

Run: `pnpm --filter piattaforma typecheck`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/piattaforma/src/auth.ts
git commit -m "feat(auth): enforce 2FA TOTP/backup code in authorize"
```

---

### Task 3: Pre-check `needTotp` in `loginAction`

**Files:** Modify `apps/piattaforma/src/app/(auth)/actions.ts`

`loginAction` deve: dopo il rate-limit, verificare la password (per individuare l'utente e sapere se ha il 2FA), e — se 2FA attivo e nessun codice fornito — ritornare `needTotp` senza loggare. Altrimenti chiamare `signIn` passando anche `totp` (authorize ri-verifica tutto).

- [ ] **Step 1: Extend the state type + imports**

Cambia:
```ts
export type LoginActionState = {
  error?: string;
};
```
in:
```ts
export type LoginActionState = {
  error?: string;
  /** true quando la password è corretta ma serve il codice 2FA. */
  needTotp?: boolean;
};
```
Aggiungi import in testa: `import bcrypt from 'bcryptjs';`

- [ ] **Step 2: Add the pre-check + pass totp to signIn**

Sostituisci il blocco `try { await signIn(...) ... }` con:

```ts
  const totp = (formData.get('totp') as string | null)?.trim() || undefined;

  // Pre-check: individua l'utente la cui password combacia (mirror di authorize)
  // per capire se il 2FA è richiesto. Non logga: serve solo a decidere se
  // mostrare il campo codice. La password NON viene mai ritornata al client.
  const candidates = await prisma.user.findMany({
    where: { email: emailLower, deletedAt: null, status: { not: 'SUSPENDED' } },
    select: { passwordHash: true, twoFactorEnabled: true },
    orderBy: [{ companyId: 'asc' }, { createdAt: 'asc' }],
  });
  let matched: (typeof candidates)[number] | null = null;
  for (const c of candidates) {
    if (await bcrypt.compare(parsed.data.password, c.passwordHash)) {
      matched = c;
      break;
    }
  }
  if (!matched) {
    return { error: 'Credenziali non valide' };
  }
  if (matched.twoFactorEnabled && !totp) {
    return { needTotp: true };
  }

  try {
    await signIn('credentials', {
      email: emailLower,
      password: parsed.data.password,
      totp,
      redirectTo: '/dashboard',
    });
    resetRateLimit(rateKey);
    return {};
  } catch (error) {
    if (error instanceof AuthError) {
      // Se il 2FA è attivo, l'errore è verosimilmente un codice errato: mantieni il campo.
      return {
        error: matched.twoFactorEnabled ? 'Codice 2FA non valido' : 'Credenziali non valide',
        needTotp: matched.twoFactorEnabled || undefined,
      };
    }
    throw error;
  }
```

> `prisma` è già importato. `signIn` con `redirectTo` lancia un redirect (NEXT_REDIRECT) in caso di successo: NON deve essere intercettato come errore — il codice esistente già rilancia tutto ciò che non è `AuthError`, quindi il redirect si propaga correttamente.

- [ ] **Step 3: Typecheck + existing test**

Run: `pnpm --filter piattaforma typecheck && pnpm --filter piattaforma test -- actions`
Expected: PASS (il test esistente di registerAction resta verde; non chiama loginAction).

- [ ] **Step 4: Commit**

```bash
git add apps/piattaforma/src/app/(auth)/actions.ts
git commit -m "feat(auth): loginAction pre-check 2FA (needTotp) + passa totp a signIn"
```

---

### Task 4: Form login — input controllati + campo codice

**Files:** Modify `apps/piattaforma/src/app/(auth)/login/login-form.tsx`

Rende email/password controllati (sopravvivono al re-render del passo 2FA) e mostra il campo codice quando `state.needTotp`.

- [ ] **Step 1: Rewrite the component**

```tsx
'use client';

import Link from 'next/link';
import { useActionState, useState } from 'react';
import { Alert, Button, Field, Input } from '@/components/ui';
import { loginAction, type LoginActionState } from '../actions';

const initialState: LoginActionState = {};

export function LoginForm() {
  const [state, formAction, pending] = useActionState(loginAction, initialState);
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');

  return (
    <div className="space-y-6">
      <div>
        <p className="text-[11px] font-bold uppercase tracking-wider text-pv-slate-500">
          Area riservata
        </p>
        <h1 className="mt-1 text-[28px] font-extrabold tracking-tight text-pv-navy-900 sm:text-[32px]">
          Accedi
        </h1>
        <p className="mt-2 text-[14px] text-pv-slate-500">
          {state.needTotp
            ? 'Inserisci il codice del tuo autenticatore (o un backup code).'
            : 'Inserisci le credenziali del tuo account.'}
        </p>
      </div>

      {state.error && <Alert variant="error">{state.error}</Alert>}

      <form action={formAction} className="space-y-4">
        <Field label="Email" htmlFor="email" required>
          <Input
            id="email"
            name="email"
            type="email"
            required
            autoComplete="email"
            placeholder="nome@azienda.it"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            readOnly={state.needTotp}
          />
        </Field>

        <Field label="Password" htmlFor="password" required>
          <Input
            id="password"
            name="password"
            type="password"
            required
            autoComplete="current-password"
            placeholder="••••••••"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            readOnly={state.needTotp}
          />
        </Field>

        {state.needTotp && (
          <Field label="Codice 2FA" htmlFor="totp" required>
            <Input
              id="totp"
              name="totp"
              type="text"
              inputMode="numeric"
              autoComplete="one-time-code"
              autoFocus
              placeholder="123456 oppure backup code"
            />
          </Field>
        )}

        <Button type="submit" loading={pending} loadingLabel="Accesso in corso…" fullWidth>
          {state.needTotp ? 'Verifica codice' : 'Accedi'}
        </Button>
      </form>

      <div className="flex items-center justify-between pt-1 text-[13px]">
        <Link
          href="/reset-password"
          className="font-semibold text-pv-navy-600 hover:underline underline-offset-4"
        >
          Password dimenticata?
        </Link>
        <Link
          href="/register"
          className="font-semibold text-pv-navy-600 hover:underline underline-offset-4"
        >
          Registra la tua azienda
        </Link>
      </div>
    </div>
  );
}
```

> Gli input controllati (`value`/`onChange`) mantengono email+password attraverso il re-render quando compare il campo 2FA — la password resta solo lato client e viene re-inviata nello stesso POST. `readOnly` sui due campi nel passo 2FA evita modifiche accidentali. Verifica che `Input` inoltri le prop `value`/`onChange`/`readOnly`/`autoFocus`/`inputMode` all'`<input>` sottostante (lo fa se fa spread di `...props`); se `Input` non accetta queste prop, adatta minimal e riporta.

- [ ] **Step 2: Typecheck + lint**

Run: `pnpm --filter piattaforma typecheck && pnpm --filter piattaforma lint`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/piattaforma/src/app/(auth)/login/login-form.tsx
git commit -m "feat(auth): login form controllato + campo codice 2FA condizionale"
```

---

### Task 5: Verifica complessiva

- [ ] **Step 1:** `pnpm --filter piattaforma test` → verde (incl. totp).
- [ ] **Step 2:** `pnpm --filter piattaforma typecheck && pnpm --filter piattaforma lint` → PASS.
- [ ] **Step 3 (manuale, se DB attivo):** utente senza 2FA → login diretto invariato. Utente con 2FA attivo → dopo email+password compare il campo codice; codice errato → "Codice 2FA non valido" (campo resta); codice corretto → accesso; backup code → accesso + il codice usato non è più valido. Chiamare `signIn` senza codice per un utente 2FA (bypass) → fallisce in authorize.

---

## Self-Review

**Spec coverage (§P4):** flusso a due passi in loginAction (needTotp) → Task 3; difesa in profondità in authorize (ricontrolla il codice, rifiuta se mancante/errato) → Task 2; consumo backup code → Task 1 (helper) + Task 2 (rimozione); UI campo codice condizionale → Task 4; nessun blocco per utenti senza 2FA → `if (matched.twoFactorEnabled)` guard ovunque. ✓

**Placeholder scan:** nessun TBD; il punto su `Input` prop forwarding è una verifica esplicita.

**Type consistency:** `verifyTwoFactor` (Task 1, firma `{secret,backupCodeHashes}, code, totpVerify?`) usato in authorize (Task 2); `LoginActionState.needTotp` (Task 3) consumato dal form (Task 4); credenziale `totp` (Task 2) ↔ `formData.get('totp')`/signIn `totp` (Task 3) ↔ input `name="totp"` (Task 4).

**Rischi:** (1) password ri-trasmessa al secondo POST e tenuta in state client — accettabile MVP (no round-trip server della password). (2) doppio bcrypt (pre-check + authorize) — costo minore accettabile. (3) backup code consumato solo in authorize (non nel pre-check) → nessuna doppia consumazione. (4) `Input` deve inoltrare `value/onChange/readOnly` — verifica in Task 4.
